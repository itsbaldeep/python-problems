# Let an list of some random numbers
list = [12, 13, 14, 11, 15]

# Using the spread operator(...)
# We pass in the elements as arguments in max method of Math object
print(max(list))

# It logs the biggest number from the list