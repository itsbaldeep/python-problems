# Let an list of some random numbers
list = [12, 13, 14, 11, 15]

# Using the spread operator(...)
# We pass in the elements as arguments in min method of Math object
print(min(list))

# It logs the smallest number from the list
